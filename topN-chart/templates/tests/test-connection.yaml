apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "topN-chart.fullname" . }}-test-connection"
  labels:
    {{- include "topN-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "topN-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
