apiVersion: v1
kind: Service
metadata:
  name: {{ include "topN-chart.fullname" . }}
  labels:
    {{- include "topN-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "topN-chart.selectorLabels" . | nindent 4 }}
